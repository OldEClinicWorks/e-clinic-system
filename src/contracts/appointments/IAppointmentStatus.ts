export interface IAppointmentStatus_DBRecord {
  id: number;
  status: string;
  description:string;
}

export interface IAppointmentStatus {
    id: number;
    title: string;
    description:string;
  }
